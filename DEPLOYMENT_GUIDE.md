# 博客项目标准化自动化部署方案

## 概述

本方案为前后端分离博客项目提供标准化、可复现的自动化部署流程，确保多实例并行不冲突。

## 核心配置

| 配置项 | 值 | 说明 |
|--------|----|------|
| 后端服务端口 | 10011 | 固定端口，仅部署此实例 |
| 实例唯一标识 | 10013 | 所有文件、目录、配置以此命名 |
| 目标服务器 | 49.235.161.106 | root 用户，SSH 密钥认证 |
| SSH 端口 | 22 | 标准 SSH 端口 |

## 部署流程总览

```
┌─────────────────────────────────────────────────────────────┐
│                     自动化部署流程                           │
├─────────────────────────────────────────────────────────────┤
│  1. 端口安全检测 → 仅终止当前项目关联进程                    │
│  2. 前端本地构建 → npm install + npm run build              │
│  3. 后端本地打包 → mvn clean package -DskipTests            │
│  4. 文件上传     → SCP 传输到服务器                         │
│  5. Nginx 配置   → 自动写入配置并验证重载                   │
│  6. 后端启动     → 优雅启动 + 健康检查                      │
│  7. 日志查看     → 实时输出启动日志                        │
│  8. 连通性测试   → 后端API + Nginx代理 + 前端页面           │
│  9. 部署报告     → 输出完整测试结果与回滚标记               │
└─────────────────────────────────────────────────────────────┘
```

## 文件结构（服务器端）

```
/opt/blog_10013/
├── backend/
│   ├── blog_10013.jar              # 后端应用包
│   ├── pid_10013                   # 进程 PID 文件
│   └── server_control.sh           # 服务控制脚本
├── frontend/                       # 前端静态资源
│   ├── index.html
│   └── assets/
├── logs/
│   ├── backend_10013.log           # 后端应用日志
│   ├── nginx_access_10013.log      # Nginx 访问日志
│   ├── nginx_error_10013.log       # Nginx 错误日志
│   └── nohup_10013.out             # 标准输出日志
└── temp/                           # 临时缓存目录

/etc/nginx/conf.d/blog_10013.conf    # Nginx 配置文件
```

## 使用方法

### 1. 完整部署

```powershell
# 执行完整部署流程
.\deploy.ps1 -Action deploy
```

### 2. 单独操作

```powershell
# 仅启动服务
.\deploy.ps1 -Action start

# 仅停止服务
.\deploy.ps1 -Action stop

# 查看实时日志
.\deploy.ps1 -Action logs

# 执行连通性测试
.\deploy.ps1 -Action test

# 执行回滚
.\deploy.ps1 -Rollback
```

### 3. 服务器端直接操作

登录服务器后，可直接使用服务控制脚本：

```bash
cd /opt/blog_10013/backend

# 查看状态
./server_control.sh status

# 启动/停止/重启
./server_control.sh start
./server_control.sh stop
./server_control.sh restart

# 查看实时日志
./server_control.sh logs

# 健康检查
./server_control.sh healthcheck
```

## 本地开发测试

```powershell
# 本地启动测试（Windows）
.\local_start.ps1
```

## 端口安全保障

1. **进程识别**：通过 Java 进程名 + Jar 包路径识别，不影响其他服务
2. **优雅停止**：先发送 TERM 信号，15 秒超时后才强制终止
3. **PID 文件**：精确管理进程生命周期，避免误杀

## 多实例隔离保证

- ✅ 所有目录名包含 `_10013` 后缀
- ✅ 所有日志文件名包含 `_10013` 后缀
- ✅ Nginx 配置文件独立命名
- ✅ PID 文件独立命名
- ✅ 后端端口严格绑定 10011

## 部署报告示例

```
========================================
          部 署 报 告
========================================
实例标识: 10013
后端端口: 10011
部署时间: 2026-04-13 21:00:00
部署服务器: 49.235.161.106
----------------------------------------
测试结果:
  ✓ Backend_API : PASS (HTTP 200)
  ✓ Nginx_Proxy : PASS (HTTP 200)
  ✓ Frontend_Page : PASS (HTTP 200)
----------------------------------------
相关路径:
  应用根目录: /opt/blog_10013
  日志目录: /opt/blog_10013/logs
  Nginx 配置: /etc/nginx/conf.d/blog_10013.conf
----------------------------------------
部署结果: ✓ 全部测试通过，部署成功！
回滚标记: deploy_20260413_210000 - 可用于回滚
========================================
```

## 注意事项

1. **严禁操作其他端口**：本脚本仅操作端口 10011，实例标识 10013
2. **SSH 密钥认证**：确保本地 SSH 密钥已添加到服务器 authorized_keys
3. **前置依赖**：本地需安装 Maven、Java、OpenSSH 客户端
4. **服务器依赖**：服务器需预装 JDK 8+、Nginx
